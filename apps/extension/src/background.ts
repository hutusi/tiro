// Deliberately minimal: clipping is driven entirely by the popup, which
// injects the clipper on demand (ADR 0005). The service worker exists so the
// extension has a stable home for future badge/context-menu features.
export {};
