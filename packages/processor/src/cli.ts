#!/usr/bin/env bun
import { TIRO_SCHEMA_VERSION } from "@tiro/shared";

// Placeholder entry point; the pipeline lands in a follow-up PR.
console.log(`tiro-process (content schema v${TIRO_SCHEMA_VERSION})`);
